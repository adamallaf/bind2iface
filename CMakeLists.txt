CMAKE_MINIMUM_REQUIRED(VERSION 3.25.0)
PROJECT("bind2iface" VERSION 0.0.1)
ADD_LIBRARY(${PROJECT_NAME} SHARED src/bind2iface.c)
SET_TARGET_PROPERTIES(
  ${PROJECT_NAME} PROPERTIES VERSION ${PROJECT_VERSION} SOVERSION 1
)
